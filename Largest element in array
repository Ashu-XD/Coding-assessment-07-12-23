// FINDING LARGEST ELEMENT
class Solution {
    public static int largest(int[] arr) {
            int large = arr[0];
            for(int i=0 ; i<arr.length ; i++)
            {
                if(arr[i]>=large)
                {
                    large = arr[i];
                }
            }
            return large;
    } 
}
