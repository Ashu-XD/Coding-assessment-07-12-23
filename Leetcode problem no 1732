int largestAltitude(int* gain, int gainSize)
{
    int maxAlt = 0, sum = 0;
    for(int i=0;i<gainSize;i++)
    {
        sum = sum+gain[i];
        if(sum>maxAlt)
        {
        maxAlt = sum;
        }
    }
    return maxAlt;
}
